# WebServ Configuration File
# Simple key=value format

# Server port
port=8080

# Server host address
host=127.0.0.1

# Root directory for serving files
root=./www

# Default index file
index=index.html
